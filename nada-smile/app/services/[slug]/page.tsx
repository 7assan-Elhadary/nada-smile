import Link from "next/link"; import {serviceDetails} from "@/lib/content"; import {notFound} from "next/navigation";
export function generateStaticParams(){return Object.keys(serviceDetails).map(slug=>({slug}))}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=serviceDetails[slug];if(!s)return notFound();return <><section className="section bg-[#DDF3EF]"><div className="container max-w-4xl"><div className="eyebrow">NADA SMILE · {s.title.toUpperCase()}</div><h1 className="display mt-5">{s.hero}</h1><p className="mt-5 text-2xl font-medium text-[#176B87]">{s.arHero}</p><p className="body mt-6 max-w-3xl">{s.body}</p></div></section><section className="section"><div className="container max-w-5xl"><div className="grid gap-4 md:grid-cols-2">{s.sections.map((x,i)=><div className="card p-7" key={x}><span className="text-sm font-bold text-[#176B87]">0{i+1}</span><h2 className="mt-5 text-xl font-semibold">{x}</h2><p className="mt-3 leading-7 text-[#536568]">{s.arSections[i]||"Care is tailored after appropriate clinical assessment."}</p></div>)}</div><div className="mt-10"><Link href="/book" className="btn btn-primary">Request an Appointment</Link></div></div></section></>}
